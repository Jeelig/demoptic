name: Release App
on:
  workflow_dispatch
  #push:
  #  tags:
  #    - v[0-9]+.[0-9]+.[0-9]+


jobs:
  namefile:
    name: 🚀 Build Filename
    runs-on: ubuntu-latest
    environment: 
      name: DEV
    steps:
      #- name: test
      #  run: echo "hello world"
      - uses: actions/checkout@v2
      - name: Update version
        uses: brettdorrans/write-version-to-file@master
        with:
          filename: '/.VERSION'
          placeholder: '${VERSION}'
          
      #- uses: pocket-apps/action-update-version@v1
      #  with:
      #    files: 'config.xml' #, app.yaml'
      #    version-regexp: '\d+.\d+.\d+'
      #    repo-token: ${{ secrets.MY_KEY }}
  

          
          
